# Flask and core
flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0
flask-migrate>=4.0.0
python-dotenv>=1.0.0

# Database drivers
psycopg2-binary>=2.9.0

# Redis
redis>=5.0.0

# WebSocket (raw gevent-websocket, no Socket.IO)
gevent>=24.2.1
gevent-websocket>=0.10.1

# HTTP client
requests>=2.31.0
httpx>=0.27.0

# Ollama/LLM integration
langchain>=0.1.0
langchain-community>=0.0.10
chromadb>=0.4.0
sentence-transformers>=2.2.0
tiktoken>=0.5.0
rank-bm25>=0.2.2
einops

# Optional LLM providers (install as needed)
openai>=1.0.0  # For OpenAI GPT models
google-generativeai>=0.3.0  # For Google Gemini models
anthropic>=0.15.0  # For Anthropic Claude models

# Auth providers
supabase>=2.0.0  # For Supabase auth (optional, needed when AUTH_PROVIDER=supabase)

# Scheduling
APScheduler>=3.10.0

# Data processing
pandas>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0
